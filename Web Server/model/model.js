class Model {
    constructor(userId, useInfo, toilet, absent) {
            this.userId = userId;
            this.useInfo = useInfo;
            this.toilet = toilet;
            this.absent = absent;
    }
}

module.exports = Model;